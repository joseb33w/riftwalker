# Riftwalker has no backend — it is fully single-player.
# The only persisted state is your best clear time, stored in the browser's localStorage.
# No environment variables are required to build or run the game.
