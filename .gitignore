# Godot
.godot/
/out/
/godot
*.tmp
*.b64

# Art + audio assets (fetched via fetch_assets.sh, not committed to keep the repo lean)
/models/
/skies/
/textures/
/audio/

# Verify tooling (never committed)
/verify/
tools_*.gd
tools_*.tscn
